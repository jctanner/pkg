name: kubectl
version: 1.35.0

variables:
  arch:
    shell: uname -m | sed -e 's/x86_64/amd64/'
  os:
    literal: linux

url: https://dl.k8s.io/release/v{{ version }}/bin/{{ os }}/{{ arch }}/kubectl
dest: ~/bin/kubectl
chmod: '0755'
